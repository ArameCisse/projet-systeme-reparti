apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: projet-sr
data:
  DJANGO_DEBUG: "1"
  DJANGO_ALLOWED_HOSTS: "*"
  CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"
  DB_ENGINE: "postgres"
  DB_NAME: "projet_sr"
  DB_HOST: "postgres"
  DB_PORT: "5432"

